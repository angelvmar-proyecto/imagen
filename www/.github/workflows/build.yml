name: Build & Deploy
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ✅ Checkout código
        uses: actions/checkout@v4

      - name: 📦 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📋 Listar archivos subidos
        run: ls -lhR

      - name: ✅ Compilación finalizada
        run: echo "🎉 TODO COMPILADO — 5 archivos Tesseract incluidos"
