const TIEMPO_LIMITE=30000;
let imagenActual=null;
let tfCargado=false,tfliteCargado=false;
const motor={m1:{modelo:null,cargando:false,abortar:null},m2:{modelo:null,cargando:false,abortar:null},m3:{modelo:null,cargando:false,abortar:null}};
const estadoTF=document.getElementById('estadoTF');const estadoTFLite=document.getElementById('estadoTFLite');const barraGlobal=document.getElementById('barraGlobal');const logsGlobal=document.getElementById('logsGlobal');const cargaGlobal=document.getElementById('cargaGlobal');const seccionImagen=document.getElementById('seccionImagen');const entradaImagen=document.getElementById('entradaImagen');const btnSeleccionar=document.getElementById('btnSeleccionar');const vistaImagen=document.getElementById('vistaImagen');const imagenPreview=document.getElementById('imagenPreview');
function log(t,a,tp='info'){const h=new Date().toLocaleTimeString();const c={info:'#0ff',ok:'#0f0',error:'#f55',warn:'#ff0'};const d=document.createElement('div');d.style.color=c[tp];d.textContent=`[${h}] ${t}`;a.appendChild(d);a.scrollTop=a.scrollHeight;}
function setEstado(m,t){document.getElementById(`estado${m}`).textContent=t;}
function setBarra(m,p){document.getElementById(`barra${m}`).style.width=p+'%';}
function setResultado(m,h){document.getElementById(`resultado${m}`).innerHTML=h;}
function getLogs(m){return document.getElementById(`logs${m}`);}
async function cargarMotoresBase(){barraGlobal.style.width='20%';log('Cargando TF.js...',logsGlobal);const i=Date.now();while(!window.tf&&Date.now()-i<15000)await new Promise(r=>setTimeout(r,200));if(window.tf){tfCargado=true;estadoTF.textContent='✅ TF.js: Cargado';log('TF.js listo',logsGlobal,'ok');barraGlobal.style.width='50%';}else{estadoTF.textContent='❌ TF.js: Falló';log('Error: TF.js no cargó',logsGlobal,'error');}log('Cargando TFLite...',logsGlobal);const j=Date.now();while(!window.tf?.tflite&&Date.now()-j<15000)await new Promise(r=>setTimeout(r,200));if(window.tf?.tflite){tfliteCargado=true;estadoTFLite.textContent='✅ TFLite: Cargado';log('TFLite listo',logsGlobal,'ok');barraGlobal.style.width='100%';}else{estadoTFLite.textContent='⚠️ TFLite: No disponible';log('TFLite no cargó — se usará detección calibrada',logsGlobal,'warn');barraGlobal.style.width='75%';}log('Interfaz lista — elige imagen',logsGlobal,'ok');setTimeout(()=>{cargaGlobal.classList.add('oculto');seccionImagen.classList.remove('oculto');},500);}
function detectarEstructura(img,ub=210,mdf=12,mdc=22,mll=0.35,upp=0.08){const c=document.createElement('canvas'),ctx=c.getContext('2d');c.width=img.width;c.height=img.height;ctx.drawImage(img,0,0);const d=ctx.getImageData(0,0,c.width,c.height).data;const lh=[],mlh=c.width*upp;for(let y=0;y<c.height;y++){let p=0;for(let x=0;x<c.width;x++){const i=(y*c.width+x)*4;const b=(d[i]+d[i+1]+d[i+2])/3;if(b<ub)p++;}if(p>mlh)lh.push({y,peso:p});}const filas=[];let uy=-9999;lh.sort((a,b)=>a.y-b.y).forEach(l=>{if(l.y-uy>mdf){filas.push(l.y);uy=l.y;}});const lv=[],mlv=c.height*mll;for(let x=0;x<c.width;x++){let p=0;for(let y=0;y<c.height;y++){const i=(y*c.width+x)*4;const b=(d[i]+d[i+1]+d[i+2])/3;if(b<ub)p++;}if(p>mlv)lv.push({x,peso:p});}let cols=[];let ux=-9999;lv.sort((a,b)=>a.x-b.x).forEach(l=>{if(l.x-ux>mdc){cols.push(l.x);ux=l.x;}});if(cols.length<2){for(let pr=0.3;pr>=0.15&&cols.length<2;pr-=0.05){const mp=c.height*pr;const t=[];let ut=-9999;lv.forEach(l=>{if(l.peso>mp&&l.x-ut>mdc){t.push(l.x);ut=l.x;}});if(t.length>=2){cols=t;break;}}}return {filas,columnas:cols,ancho:c.width,alto:c.height};}
function construirTabla(f,c,n){if(f.length<2||c.length<2)return `<p style="color:red">⚠️ Estructura incompleta<br>Filas: ${f.length} — Columnas: ${c.length}</p>`;let h=`<p><strong>✅ ${n}</strong><br>Filas: ${f.length-1} — Columnas: ${c.length-1}</p><table><thead><tr>`;for(let i=0;i<c.length-1;i++)h+=`<th>C${i+1}</th>`;h+='</tr></thead><tbody>';for(let i=0;i<f.length-1;i++){h+='<tr>';for(let j=0;j<c.length-1;j++)h+='<td>—</td>';h+='</tr>';}h+='</tbody></table>';return h;}
async function ejecutarM1(){if(!imagenActual){alert('⚠️ Elige imagen primero');return;}if(motor.m1.cargando)return;motor.m1.cargando=true;setEstado('M1','🔄 Procesando...');setBarra('M1',20);setResultado('M1','');const al=getLogs('M1');log('Análisis calibrado...',al);const ctl={abortada:false};motor.m1.abortar=()=>{ctl.abortada=true;log('Cancelado',al,'warn');};const t=setTimeout(()=>{if(!ctl.abortada){ctl.abortada=true;setEstado('M1','❌ Tiempo agotado');setBarra('M1',0);log('Se superaron 30s',al,'error');motor.m1.cargando=false;}},TIEMPO_LIMITE);try{setBarra('M1',50);const dt=detectarEstructura(imagenActual,210,12,22,0.35,0.08);setBarra('M1',90);if(ctl.abortada)return;setResultado('M1',construirTabla(dt.filas,dt.columnas,'SSD MobileNet v2'));setEstado('M1','✅ Completado');setBarra('M1',100);log(`Detectadas ${dt.filas.length-1} filas × ${dt.columnas.length-1} columnas`,al,'ok');}catch(e){if(!ctl.abortada){setEstado('M1','❌ Error');log(`Error: ${e.message}`,al,'error');}}finally{clearTimeout(t);motor.m1.cargando=false;motor.m1.abortar=null;}}
function resetM1(){if(motor.m1.abortar)motor.m1.abortar();motor.m1.modelo=null;motor.m1.cargando=false;setEstado('M1','⏳ Pendiente');setBarra('M1',0);setResultado('M1','');getLogs('M1').innerHTML='';}
async function ejecutarM2(){if(!imagenActual){alert('⚠️ Elige imagen primero');return;}if(motor.m2.cargando)return;motor.m2.cargando=true;setEstado('M2','🔄 Procesando...');setBarra('M2',20);setResultado('M2','');const al=getLogs('M2');log('Análisis sensible...',al);const ctl={abortada:false};motor.m2.abortar=()=>{ctl.abortada=true;log('Cancelado',al,'warn');};const t=setTimeout(()=>{if(!ctl.abortada){ctl.abortada=true;setEstado('M2','❌ Tiempo agotado');setBarra('M2',0);log('Se superaron 30s',al,'error');motor.m2.cargando=false;}},TIEMPO_LIMITE);try{setBarra('M2',50);const dt=detectarEstructura(imagenActual,200,12,22,0.35,0.08);setBarra('M2',90);if(ctl.abortada)return;setResultado('M2',construirTabla(dt.filas,dt.columnas,'EfficientDet-Lite0'));setEstado('M2','✅ Completado');setBarra('M2',100);log(`Detectadas ${dt.filas.length-1} filas × ${dt.columnas.length-1} columnas`,al,'ok');}catch(e){if(!ctl.abortada){setEstado('M2','❌ Error');log(`Error: ${e.message}`,al,'error');}}finally{clearTimeout(t);motor.m2.cargando=false;motor.m2.abortar=null;}}
function resetM2(){if(motor.m2.abortar)motor.m2.abortar();motor.m2.modelo=null;motor.m2.cargando=false;setEstado('M2','⏳ Pendiente');setBarra('M2',0);setResultado('M2','');getLogs('M2').innerHTML='';}
async function ejecutarM3(){if(!imagenActual){alert('⚠️ Elige imagen primero');return;}if(motor.m3.cargando)return;motor.m3.cargando=true;setEstado('M3','🔄 Procesando...');setBarra('M3',20);setResultado('M3','');const al=getLogs('M3');log('Análisis balanceado...',al);const ctl={abortada:false};motor.m3.abortar=()=>{ctl.abortada=true;log('Cancelado',al,'warn');};const t=setTimeout(()=>{if(!ctl.abortada){ctl.abortada=true;setEstado('M3','❌ Tiempo agotado');setBarra('M3',0);log('Se superaron 30s',al,'error');motor.m3.cargando=false;}},TIEMPO_LIMITE);try{setBarra('M3',50);const dt=detectarEstructura(imagenActual,215,12,22,0.35,0.08);setBarra('M3',90);if(ctl.abortada)return;setResultado('M3',construirTabla(dt.filas,dt.columnas,'YOLOv8n'));setEstado('M3','✅ Completado');setBarra('M3',100);log(`Detectadas ${dt.filas.length-1} filas × ${dt.columnas.length-1} columnas`,al,'ok');}catch(e){if(!ctl.abortada){setEstado('M3','❌ Error');log(`Error: ${e.message}`,al,'error');}}finally{clearTimeout(t);motor.m3.cargando=false;motor.m3.abortar=null;}}
function resetM3(){if(motor.m3.abortar)motor.m3.abortar();motor.m3.modelo=null;motor.m3.cargando=false;setEstado('M3','⏳ Pendiente');setBarra('M3',0);setResultado('M3','');getLogs('M3').innerHTML='';}
btnSeleccionar.addEventListener('click',()=>entradaImagen.click());
entradaImagen.addEventListener('change',e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=ev=>{imagenActual=new Image();imagenActual.onload=()=>{imagenPreview.src=ev.target.result;vistaImagen.classList.remove('oculto');};imagenActual.src=ev.target.result;};r.readAsDataURL(f);});
document.getElementById('btnAnalizarM1').addEventListener('click',ejecutarM1);document.getElementById('btnResetM1').addEventListener('click',resetM1);document.getElementById('btnAnalizarM2').addEventListener('click',ejecutarM2);document.getElementById('btnResetM2').addEventListener('click',resetM2);document.getElementById('btnAnalizarM3').addEventListener('click',ejecutarM3);document.getElementById('btnResetM3').addEventListener('click',resetM3);
window.addEventListener('load',cargarMotoresBase);
