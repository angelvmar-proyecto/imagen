{
  "name": "mar-caribe-escanner",
  "version": "1.0.0",
  "description": "Escáner de tablas MAR Caribe con OCR",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Build completo'",
    "cap:sync": "cap sync android",
    "cap:add": "cap add android",
    "cap:build": "cap build android"
  },
  "dependencies": {},
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
