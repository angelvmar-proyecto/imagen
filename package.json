{
  "name": "mar-caribe-ocr",
  "version": "1.0.0",
  "description": "Escáner OCR de Tablas — MAR Caribe",
  "scripts": {
    "dev": "npx serve www",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/camera": "^6.0.0",
    "@capacitor/clipboard": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "tesseract.js": "^4.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^7.0.2"
  }
}
