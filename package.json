{
  "name": "mar-caribe",
  "version": "1.0.0",
  "description": "MAR Caribe — Modelos locales independientes",
  "main": "www/index.html",
  "scripts": {
    "sync": "npx cap sync android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@tensorflow/tfjs": "^4.20.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
