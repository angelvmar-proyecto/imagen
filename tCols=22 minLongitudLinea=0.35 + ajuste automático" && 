[1mdiff --git a/www/js/app.js b/www/js/app.js[m
[1mindex 2e0dfe9..416a6fe 100644[m
[1m--- a/www/js/app.js[m
[1m+++ b/www/js/app.js[m
[36m@@ -33,257 +33,206 @@[m [mfunction setBarra(m,pct){document.getElementById(`barra${m}`).style.width=pct+'%[m
 function setResultado(m,html){document.getElementById(`resultado${m}`).innerHTML=html;}[m
 function getLogs(m){return document.getElementById(`logs${m}`);}[m
 [m
[31m-// ==================================================[m
[31m-// CARGA DE LIBRERÍAS — TAL CUAL ESTABA[m
[31m-// ==================================================[m
 async function cargarMotoresBase(){[m
   barraGlobal.style.width='20%';[m
[31m-  [m
   log('Cargando TF.js...',logsGlobal,'info');[m
   const inicioTF=Date.now();[m
   while(!window.tf && Date.now()-inicioTF<15000) await new Promise(r=>setTimeout(r,200));[m
   if(window.tf){[m
[31m-    tfCargado=true;[m
[31m-    estadoTF.textContent='✅ TF.js: Cargado';[m
[31m-    log('TF.js listo',logsGlobal,'ok');[m
[31m-    barraGlobal.style.width='50%';[m
[32m+[m[32m    tfCargado=true; estadoTF.textContent='✅ TF.js: Cargado';[m
[32m+[m[32m    log('TF.js listo',logsGlobal,'ok'); barraGlobal.style.width='50%';[m
   }else{[m
[31m-    estadoTF.textContent='❌ TF.js: Falló';[m
[31m-    log('Error: TF.js no cargó',logsGlobal,'error');[m
[32m+[m[32m    estadoTF.textContent='❌ TF.js: Falló'; log('Error: TF.js no cargó',logsGlobal,'error');[m
   }[m
 [m
   log('Cargando TFLite...',logsGlobal,'info');[m
   const inicioTFLite=Date.now();[m
   while(!window.tf?.tflite && Date.now()-inicioTFLite<15000) await new Promise(r=>setTimeout(r,200));[m
   if(window.tf?.tflite){[m
[31m-    tfliteCargado=true;[m
[31m-    estadoTFLite.textContent='✅ TFLite: Cargado';[m
[31m-    log('TFLite listo',logsGlobal,'ok');[m
[31m-    barraGlobal.style.width='100%';[m
[32m+[m[32m    tfliteCargado=true; estadoTFLite.textContent='✅ TFLite: Cargado';[m
[32m+[m[32m    log('TFLite listo',logsGlobal,'ok'); barraGlobal.style.width='100%';[m
   }else{[m
     estadoTFLite.textContent='⚠️ TFLite: No disponible';[m
[31m-    log('TFLite no cargó — se usará detección de líneas mejorada',logsGlobal,'warn');[m
[32m+[m[32m    log('TFLite no cargó — se usará detección calibrada',logsGlobal,'warn');[m
     barraGlobal.style.width='75%';[m
   }[m
 [m
[31m-  log('Interfaz lista — puedes elegir imagen ahora',logsGlobal,'ok');[m
[31m-  setTimeout(()=>{[m
[31m-    cargaGlobal.classList.add('oculto');[m
[31m-    seccionImagen.classList.remove('oculto');[m
[31m-  },500);[m
[32m+[m[32m  log('Interfaz lista — elige una imagen',logsGlobal,'ok');[m
[32m+[m[32m  setTimeout(()=>{cargaGlobal.classList.add('oculto');seccionImagen.classList.remove('oculto');},500);[m
 }[m
 [m
[31m-// ==================================================[m
[31m-// DETECCIÓN CORREGIDA — SOLO AJUSTADO, SIN QUITAR NADA[m
[31m-// ==================================================[m
 function detectarEstructura([m
   img,[m
[31m-  umbral=210,[m
[31m-  minDistFilas=15,[m
[31m-  minDistCols=40,       // ✅ AUMENTADO: ignorar líneas pegadas[m
[31m-  minLongitudLinea=0.85 // ✅ SOLO líneas largas reales[m
[32m+[m[32m  umbralBrillo=210,[m
[32m+[m[32m  minDistFilas=12,[m
[32m+[m[32m  minDistCols=22,[m
[32m+[m[32m  minLongitudLinea=0.35,[m
[32m+[m[32m  umbralPixelesPct=0.08[m
 ){[m
   const c=document.createElement('canvas'),ctx=c.getContext('2d');[m
   c.width=img.width; c.height=img.height;[m
   ctx.drawImage(img,0,0);[m
   const datos=ctx.getImageData(0,0,c.width,c.height).data;[m
 [m
[31m-  // LÍNEAS HORIZONTALES → FILAS — TAL CUAL[m
[31m-  const h=[];[m
[31m-  const umbralPixelesH=c.width*0.10;[m
[32m+[m[32m  const lineasHoriz=[];[m
[32m+[m[32m  const minPixelesLineaH=c.width*umbralPixelesPct;[m
   for(let y=0;y<c.height;y++){[m
[31m-    let oscuro=0;[m
[31m-    for(let x=0;x<c.width;x+=2){[m
[32m+[m[32m    let pixelesOscuros=0;[m
[32m+[m[32m    for(let x=0;x<c.width;x++){[m
       const i=(y*c.width+x)*4;[m
       const brillo=(datos[i]+datos[i+1]+datos[i+2])/3;[m
[31m-      if(brillo<umbral) oscuro++;[m
[32m+[m[32m      if(brillo<umbralBrillo) pixelesOscuros++;[m
     }[m
[31m-    if(oscuro>umbralPixelesH) h.push({y,peso:oscuro});[m
[32m+[m[32m    if(pixelesOscuros>minPixelesLineaH) lineasHoriz.push({y, peso:pixelesOscuros});[m
   }[m
[32m+[m[32m  const filas=[]; let ultimaY=-9999;[m
[32m+[m[32m  lineasHoriz.sort((a,b)=>a.y-b.y).forEach(linea=>{[m
[32m+[m[32m    if(linea.y-ultimaY>minDistFilas){ filas.push(linea.y); ultimaY=linea.y; }[m
[32m+[m[32m  });[m
 [m
[31m-  // LÍNEAS VERTICALES → COLUMNAS — CON FILTRO DE LONGITUD[m
[31m-  const v=[];[m
[31m-  const umbralPixelesV=c.height*minLongitudLinea; // ✅ Solo líneas largas[m
[32m+[m[32m  const lineasVert=[];[m
[32m+[m[32m  const minPixelesLineaV=c.height*minLongitudLinea;[m
   for(let x=0;x<c.width;x++){[m
[31m-    let oscuro=0;[m
[31m-    for(let y=0;y<c.height;y+=2){[m
[32m+[m[32m    let pixelesOscuros=0;[m
[32m+[m[32m    for(let y=0;y<c.height;y++){[m
       const i=(y*c.width+x)*4;[m
       const brillo=(datos[i]+datos[i+1]+datos[i+2])/3;[m
[31m-      if(brillo<umbral) oscuro++;[m
[32m+[m[32m      if(brillo<umbralBrillo) pixelesOscuros++;[m
     }[m
[31m-    if(oscuro>umbralPixelesV) v.push({x,peso:oscuro}); // ✅ No cuenta sombras/cortas[m
[32m+[m[32m    if(pixelesOscuros>minPixelesLineaV) lineasVert.push({x, peso:pixelesOscuros});[m
   }[m
[31m-[m
[31m-  // AGRUPAR FILAS — TAL CUAL[m
[31m-  const filas=[];[m
[31m-  let ultY=-9999;[m
[31m-  h.sort((a,b)=>a.y-b.y).forEach(linea=>{[m
[31m-    if(linea.y-ultY>minDistFilas){[m
[31m-      filas.push(linea.y);[m
[31m-      ultY=linea.y;[m
[31m-    }[m
[32m+[m[32m  let columnas=[]; let ultimaX=-9999;[m
[32m+[m[32m  lineasVert.sort((a,b)=>a.x-b.x).forEach(linea=>{[m
[32m+[m[32m    if(linea.x-ultimaX>minDistCols){ columnas.push(linea.x); ultimaX=linea.x; }[m
   });[m
 [m
[31m-  // AGRUPAR COLUMNAS — ✅ IGNORA LO PEGADO[m
[31m-  const cols=[];[m
[31m-  let ultX=-9999;[m
[31m-  v.sort((a,b)=>a.x-b.x).forEach(linea=>{[m
[31m-    if(linea.x-ultX>minDistCols){ // ✅ Solo separadas, no pegadas[m
[31m-      cols.push(linea.x);[m
[31m-      ultX=linea.x;[m
[32m+[m[32m  if(columnas.length<2){[m
[32m+[m[32m    for(let prueba=0.30; prueba>=0.15 && columnas.length<2; prueba-=0.05){[m
[32m+[m[32m      const minPrueba=c.height*prueba;[m
[32m+[m[32m      const temp=[]; let ult=-9999;[m
[32m+[m[32m      lineasVert.forEach(l=>{[m
[32m+[m[32m        if(l.peso>minPrueba && l.x-ult>minDistCols){ temp.push(l.x); ult=l.x; }[m
[32m+[m[32m      });[m
[32m+[m[32m      if(temp.length>=2){ columnas=temp; break; }[m
     }[m
[31m-  });[m
[32m+[m[32m  }[m
 [m
[31m-  return {filas,cols,ancho:c.width,alto:c.height};[m
[32m+[m[32m  return {filas, columnas, ancho:c.width, alto:c.height};[m
 }[m
 [m
[31m-function construirTablaResultado(filas,cols,nombre){[m
[31m-  if(filas.length<2||cols.length<2){[m
[31m-    return `<p style="color:red">⚠️ Estructura no detectada completa<br>Filas: ${filas.length} — Columnas: ${cols.length}</p>`;[m
[32m+[m[32mfunction construirTablaResultado(filas,columnas,nombre){[m
[32m+[m[32m  if(filas.length<2||columnas.length<2){[m
[32m+[m[32m    return `<p style="color:red">⚠️ Estructura no detectada completa<br>Filas: ${filas.length} — Columnas: ${columnas.length}</p>`;[m
   }[m
[31m-  let html=`<p><strong>✅ ${nombre}</strong><br>Filas: ${filas.length-1} — Columnas: ${cols.length-1}</p>`;[m
[32m+[m[32m  let html=`<p><strong>✅ ${nombre}</strong><br>Filas: ${filas.length-1} — Columnas: ${columnas.length-1}</p>`;[m
   html+='<table><thead><tr>';[m
[31m-  for(let c=0;c<cols.length-1;c++) html+=`<th>C${c+1}</th>`;[m
[32m+[m[32m  for(let c=0;c<columnas.length-1;c++) html+=`<th>C${c+1}</th>`;[m
   html+='</tr></thead><tbody>';[m
   for(let f=0;f<filas.length-1;f++){[m
     html+='<tr>';[m
[31m-    for(let c=0;c<cols.length-1;c++) html+='<td>—</td>';[m
[32m+[m[32m    for(let c=0;c<columnas.length-1;c++) html+='<td>—</td>';[m
     html+='</tr>';[m
   }[m
   html+='</tbody></table>';[m
   return html;[m
 }[m
 [m
[31m-// ==================================================[m
[31m-// MOTOR 1 — TAL CUAL ESTABA, SOLO PARÁMETROS AJUSTADOS[m
[31m-// ==================================================[m
 async function ejecutarM1(){[m
   if(!imagenActual){alert('⚠️ Elige una imagen primero');return;}[m
   if(motor.m1.cargando) return;[m
   motor.m1.cargando=true;[m
[31m-  setEstado('M1','🔄 Procesando...');[m
[31m-  setBarra('M1',20);[m
[31m-  setResultado('M1','');[m
[32m+[m[32m  setEstado('M1','🔄 Procesando...'); setBarra('M1',20); setResultado('M1','');[m
   const areaLog=getLogs('M1');[m
[31m-  log('Iniciando análisis con parámetros estándar...',areaLog,'info');[m
[32m+[m[32m  log('Análisis con umbrales calibrados...',areaLog,'info');[m
 [m
   const control={abortada:false};[m
   motor.m1.abortar=()=>{control.abortada=true;log('Cancelado por Reset',areaLog,'warn');};[m
[31m-[m
   const tiempoLimite=setTimeout(()=>{[m
[31m-    if(!control.abortada){[m
[31m-      control.abortada=true;[m
[31m-      setEstado('M1','❌ Tiempo agotado');[m
[31m-      setBarra('M1',0);[m
[31m-      log('Se superaron los 30s',areaLog,'error');[m
[31m-      motor.m1.cargando=false;[m
[31m-    }[m
[32m+[m[32m    if(!control.abortada){control.abortada=true;setEstado('M1','❌ Tiempo agotado');setBarra('M1',0);log('Se superaron los 30s',areaLog,'error');motor.m1.cargando=false;}[m
   },TIEMPO_LIMITE);[m
 [m
   try{[m
     setBarra('M1',50);[m
[31m-    const datos=detectarEstructura(imagenActual,210,15,40,0.85);[m
[32m+[m[32m    const datos=detectarEstructura(imagenActual,210,12,22,0.35,0.08);[m
     setBarra('M1',90);[m
     if(control.abortada) return;[m
[31m-    setResultado('M1',construirTablaResultado(datos.filas,datos.cols,'SSD MobileNet v2'));[m
[31m-    setEstado('M1','✅ Completado');[m
[31m-    setBarra('M1',100);[m
[31m-    log(`Detectadas ${datos.filas.length-1} filas × ${datos.cols.length-1} columnas`,areaLog,'ok');[m
[32m+[m[32m    setResultado('M1',construirTablaResultado(datos.filas,datos.columnas,'SSD MobileNet v2'));[m
[32m+[m[32m    setEstado('M1','✅ Completado'); setBarra('M1',100);[m
[32m+[m[32m    log(`Detectadas ${datos.filas.length-1} filas × ${datos.columnas.length-1} columnas`,areaLog,'ok');[m
   }catch(e){[m
     if(!control.abortada){setEstado('M1','❌ Error');log(`Error: ${e.message}`,areaLog,'error');}[m
   }finally{clearTimeout(tiempoLimite);motor.m1.cargando=false;motor.m1.abortar=null;}[m
 }[m
 function resetM1(){if(motor.m1.abortar)motor.m1.abortar();motor.m1.modelo=null;motor.m1.cargando=false;setEstado('M1','⏳ Pendiente');setBarra('M1',0);setResultado('M1','');getLogs('M1').innerHTML='';}[m
 [m
[31m-// ==================================================[m
[31m-// MOTOR 2 — TAL CUAL ESTABA[m
[31m-// ==================================================[m
 async function ejecutarM2(){[m
   if(!imagenActual){alert('⚠️ Elige una imagen primero');return;}[m
   if(motor.m2.cargando) return;[m
   motor.m2.cargando=true;[m
[31m-  setEstado('M2','🔄 Procesando...');[m
[31m-  setBarra('M2',20);[m
[31m-  setResultado('M2','');[m
[32m+[m[32m  setEstado('M2','🔄 Procesando...'); setBarra('M2',20); setResultado('M2','');[m
   const areaLog=getLogs('M2');[m
[31m-  log('Iniciando análisis con umbral más sensible...',areaLog,'info');[m
[32m+[m[32m  log('Análisis con umbral más sensible...',areaLog,'info');[m
 [m
   const control={abortada:false};[m
   motor.m2.abortar=()=>{control.abortada=true;log('Cancelado por Reset',areaLog,'warn');};[m
[31m-[m
   const tiempoLimite=setTimeout(()=>{[m
     if(!control.abortada){control.abortada=true;setEstado('M2','❌ Tiempo agotado');setBarra('M2',0);log('Se superaron los 30s',areaLog,'error');motor.m2.cargando=false;}[m
   },TIEMPO_LIMITE);[m
 [m
   try{[m
     setBarra('M2',50);[m
[31m-    const datos=detectarEstructura(imagenActual,200,12,45,0.88);[m
[32m+[m[32m    const datos=detectarEstructura(imagenActual,200,12,22,0.35,0.08);[m
     setBarra('M2',90);[m
     if(control.abortada) return;[m
[31m-    setResultado('M2',construirTablaResultado(datos.filas,datos.cols,'EfficientDet-Lite0'));[m
[31m-    setEstado('M2','✅ Completado');[m
[31m-    setBarra('M2',100);[m
[31m-    log(`Detectadas ${datos.filas.length-1} filas × ${datos.cols.length-1} columnas`,areaLog,'ok');[m
[32m+[m[32m    setResultado('M2',construirTablaResultado(datos.filas,datos.columnas,'EfficientDet-Lite0'));[m
[32m+[m[32m    setEstado('M2','✅ Completado'); setBarra('M2',100);[m
[32m+[m[32m    log(`Detectadas ${datos.filas.length-1} filas × ${datos.columnas.length-1} columnas`,areaLog,'ok');[m
   }catch(e){[m
     if(!control.abortada){setEstado('M2','❌ Error');log(`Error: ${e.message}`,areaLog,'error');}[m
   }finally{clearTimeout(tiempoLimite);motor.m2.cargando=false;motor.m2.abortar=null;}[m
 }[m
 function resetM2(){if(motor.m2.abortar)motor.m2.abortar();motor.m2.modelo=null;motor.m2.cargando=false;setEstado('M2','⏳ Pendiente');setBarra('M2',0);setResultado('M2','');getLogs('M2').innerHTML='';}[m
 [m
[31m-// ==================================================[m
[31m-// MOTOR 3 — TAL CUAL ESTABA, NO LO QUITÉ[m
[31m-// ==================================================[m
 async function ejecutarM3(){[m
   if(!imagenActual){alert('⚠️ Elige una imagen primero');return;}[m
   if(motor.m3.cargando) return;[m
   motor.m3.cargando=true;[m
[31m-  setEstado('M3','🔄 Procesando...');[m
[31m-  setBarra('M3',20);[m
[31m-  setResultado('M3','');[m
[32m+[m[32m  setEstado('M3','🔄 Procesando...'); setBarra('M3',20); setResultado('M3','');[m
   const areaLog=getLogs('M3');[m
[31m-  log('Iniciando análisis con parámetros balanceados...',areaLog,'info');[m
[32m+[m[32m  log('Análisis con parámetros balanceados...',areaLog,'info');[m
 [m
   const control={abortada:false};[m
   motor.m3.abortar=()=>{control.abortada=true;log('Cancelado por Reset',areaLog,'warn');};[m
[31m-[m
   const tiempoLimite=setTimeout(()=>{[m
     if(!control.abortada){control.abortada=true;setEstado('M3','❌ Tiempo agotado');setBarra('M3',0);log('Se superaron los 30s',areaLog,'error');motor.m3.cargando=false;}[m
   },TIEMPO_LIMITE);[m
 [m
   try{[m
     setBarra('M3',50);[m
[31m-    const datos=detectarEstructura(imagenActual,215,14,50,0.90);[m
[32m+[m[32m    const datos=detectarEstructura(imagenActual,215,12,22,0.35,0.08);[m
     setBarra('M3',90);[m
     if(control.abortada) return;[m
[31m-    setResultado('M3',construirTablaResultado(datos.filas,datos.cols,'YOLOv8n'));[m
[31m-    setEstado('M3','✅ Completado');[m
[31m-    setBarra('M3',100);[m
[31m-    log(`Detectadas ${datos.filas.length-1} filas × ${datos.cols.length-1} columnas`,areaLog,'ok');[m
[32m+[m[32m    setResultado('M3',construirTablaResultado(datos.filas,datos.columnas,'YOLOv8n'));[m
[32m+[m[32m    setEstado('M3','✅ Completado'); setBarra('M3',100);[m
[32m+[m[32m    log(`Detectadas ${datos.filas.length-1} filas × ${datos.columnas.length-1} columnas`,areaLog,'ok');[m
   }catch(e){[m
     if(!control.abortada){setEstado('M3','❌ Error');log(`Error: ${e.message}`,areaLog,'error');}[m
   }finally{clearTimeout(tiempoLimite);motor.m3.cargando=false;motor.m3.abortar=null;}[m
 }[m
 function resetM3(){if(motor.m3.abortar)motor.m3.abortar();motor.m3.modelo=null;motor.m3.cargando=false;setEstado('M3','⏳ Pendiente');setBarra('M3',0);setResultado('M3','');getLogs('M3').innerHTML='';}[m
 [m
[31m-// ==================================================[m
[31m-// SELECCIÓN DE IMAGEN — TAL CUAL ESTABA[m
[31m-// ==================================================[m
 btnSeleccionar.addEventListener('click',()=>entradaImagen.click());[m
 entradaImagen.addEventListener('change',e=>{[m
   const f=e.target.files[0];if(!f)return;[m
   const r=new FileReader();[m
   r.onload=evt=>{[m
     imagenActual=new Image();[m
[31m-    imagenActual.onload=()=>{[m
[31m-      imagenPreview.src=evt.target.result;[m
[31m-      vistaImagen.classList.remove('oculto');[m
[31m-    };[m
[32m+[m[32m    imagenActual.onload=()=>{imagenPreview.src=evt.target.result;vistaImagen.classList.remove('oculto');};[m
     imagenActual.src=evt.target.result;[m
   };[m
   r.readAsDataURL(f);[m
 });[m
 [m
[31m-// BOTONES — TAL CUAL[m
 document.getElementById('btnAnalizarM1').addEventListener('click',ejecutarM1);[m
 document.getElementById('btnResetM1').addEventListener('click',resetM1);[m
 document.getElementById('btnAnalizarM2').addEventListener('click',ejecutarM2);[m
