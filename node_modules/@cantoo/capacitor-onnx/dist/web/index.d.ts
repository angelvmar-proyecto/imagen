import type { CapacitorOnnxPlugin, LoadModelInput, LoadModelResult, ReleaseModelInput, RunInput, RunResult } from "../definitions";
export declare class CapacitorOnnxWeb implements CapacitorOnnxPlugin {
    private sessions;
    private static modelKey;
    loadModel(_input: LoadModelInput): Promise<LoadModelResult>;
    run(_input: RunInput): Promise<RunResult>;
    release(_input: ReleaseModelInput): Promise<void>;
    private static warmupSession;
}
