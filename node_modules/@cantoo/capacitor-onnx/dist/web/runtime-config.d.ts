export declare function applyWebRuntimeConfig(wasmPath?: string, proxy?: boolean): void;
/**
 * Single-threaded mode with SIMD enabled. Use for models small enough that
 * thread-pool overhead outweighs the parallelism gain.
 */
export declare function applySingleThreadRuntime(): void;
export declare function applyRuntimeThreads(numThreads?: number): void;
