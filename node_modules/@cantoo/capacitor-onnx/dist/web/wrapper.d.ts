import { WebPlugin } from "@capacitor/core";
import type { CapacitorOnnxPlugin, LoadModelInput, LoadModelResult, ReleaseModelInput, RunInput, RunResult } from "../definitions";
export declare class WrapperCapacitorOnnxWeb extends WebPlugin implements CapacitorOnnxPlugin {
    private readonly engine;
    loadModel(input: LoadModelInput): Promise<LoadModelResult>;
    run(input: RunInput): Promise<RunResult>;
    release(input: ReleaseModelInput): Promise<void>;
}
