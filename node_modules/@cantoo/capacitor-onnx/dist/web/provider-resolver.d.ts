import * as ort from "onnxruntime-web";
import type { SessionOptionsInput } from "../definitions";
export type WebExecutionProvider = "wasm" | "webgpu" | "webnn";
export declare function createSessionWithFallback(model: Uint8Array, requestedProvider: SessionOptionsInput["executionProvider"] | undefined): Promise<{
    session: ort.InferenceSession;
    providerUsed: WebExecutionProvider;
}>;
